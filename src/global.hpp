#ifndef ISLAND_GLOBAL_HPP
#define ISLAND_GLOBAL_HPP

#include <QString>

namespace Island {
  enum Mode {
    NormalMode,
    InsertMode,
    CommandMode,
    SearchForwardMode,
    SearchBackwardMode,
  };
};

#endif
